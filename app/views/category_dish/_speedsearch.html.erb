<% item.each do |items|%>
  <a href="<%= category_dish_path(items.id)%>" class="btn badge-info badge-pill pt-3 active col-sm-auto w-auto mx-1 mb-2" role="button" aria-pressed="true">
    <%= items.name%>
  </a>
  <%end%>