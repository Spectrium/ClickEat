<% item.each do |items|%>
    <a href="<%= speciality_path(items.id)%>" class="badge badge-info badge-pill pt-3 active col-sm-auto w-auto mx-1 mb-2" role="button" aria-pressed="true">
        <%= items.name%>
    </a>
    <%end%>