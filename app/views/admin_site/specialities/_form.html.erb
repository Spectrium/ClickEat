<%= form_with(model: speciality, local: true, url:(speciality.persisted? ? admin_site_speciality_path(speciality) : admin_site_specialities_path )) do |form| %>
  <% if speciality.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(speciality.errors.count, "error") %> prohibited this speciality from being saved:</h2>
      <ul>
        <% speciality.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>
      <%= form.label :name do %>
        Catégorie de spécialité <span style="color:red;">*</span>:
        <% end %>
          <%= form.select :category_id, all_specialities_categories.collect { |speciality| [speciality.name, speciality.id]} ,class:'form-control' %>
            <br>
            <br>
            <%= form.label :name do %>
              Nom de la nouvelle spécialité <span style="color:red;">*</span>:
              <% end %>
                <%= form.text_field :name, class:'form-control' %>
                  <br>
                  <br>
                  <%= form.label :password do %>
                    Mot de passe<span style="color:red;">*</span>:
                    <% end %>
                      <%= form.password_field :password, class:'form-control' %>
                        <br>
                        <br>
                        <%= form.submit :valider, class:"btn btn-primary" %>

                          <% end %>