<%= form_with(model: type, local: true, url:(type.persisted? ? admin_site_type_path(type) : admin_site_types_path )) do |form| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(type.errors.count, "error") %> prohibited this speciality from being saved:</h2>
      <ul>
        <% type.errors.full_messages.each do |message| %>
          <li>
            <%= message %>
          </li>
          <% end %>
      </ul>
    </div>
    <% end %>
      <%= form.label :name do %>
        Nom du nouveau type <span style="color:red;">*</span>:
        <% end %>
          <%= form.text_field :name ,class:'form-control' %>
            <br>
            <br>
            <%= form.label :password do %>
              Mot de passe<span style="color:red;">*</span>:
              <% end %>
                <%= form.password_field :password, class:'form-control' %>
                  <br>
                  <br>
                  <%= form.submit :valider, class:"btn btn-primary" %>

                    <% end %>